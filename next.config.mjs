/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com','media.istockphoto.com',"www.shutterstock.com","encrypted-tbn0.gstatic.com"],
      },
    
};

export default nextConfig;
